{
  "name": "fit2102",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "register-kernals": "npx tslab install --sys-prefix --python=python",
    "postinstall": "npm run register-kernals"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "tslab": "^1.0.22"
  }
}
